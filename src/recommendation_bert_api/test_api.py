# test_bert_api.py
import requests
import json
import time
import random

# Базовый URL API
BASE_URL = "http://localhost:8000"

# Расширенные квесты с более подробными описаниями
quests_data = [
    # Здоровье и фитнес
    {
        "id": 1,
        "title": "Утренний дружеский марафон",
        "description": "Совместный недельный челлендж для развития силы воли и здоровья. Ежедневные утренние пробежки, здоровое питание и медитация. Идеально для тех, кто хочет начать день с энергии и бодрости. Включает в себя трекинг прогресса и поддержку сообщества.",
        "category": "health"
    },
    {
        "id": 2,
        "title": "Фитнес-трансформация за 30 дней",
        "description": "Комплексная программа тренировок, питания и восстановления. Индивидуальные планы упражнений, рецепты здоровых блюд и техники для улучшения сна. Подходит как для новичков, так и для опытных спортсменов.",
        "category": "health"
    },
    {
        "id": 3,
        "title": "Йога для начинающих: 21 день практики",
        "description": "Постепенное погружение в мир йоги с базовых асан до простых последовательностей. Улучшение гибкости, снижение стресса и обретение внутреннего баланса. Ежедневные видеоуроки и дневник прогресса.",
        "category": "health"
    },
    {
        "id": 4,
        "title": "Беговой клуб: от 0 до 5км",
        "description": "Программа подготовки к первому забегу на 5 километров. Интервальные тренировки, техника бега, растяжка и питание для бегунов. Виртуальные групповые забеги по выходным.",
        "category": "health"
    },

    # Продуктивность и сила воли
    {
        "id": 5,
        "title": "Основы продуктивности",
        "description": "Недельный план для формирования полезных привычек и повышения эффективности. Техники тайм-менеджмента, фокус-сессии, борьба с прокрастинацией. Инструменты для планирования и отслеживания задач.",
        "category": "willpower"
    },
    {
        "id": 6,
        "title": "Цифровой детокс: 7 дней без соцсетей",
        "description": "Освобождение от цифровой зависимости и возвращение контроля над временем. Практики осознанного использования технологий, альтернативные активности, восстановление концентрации внимания.",
        "category": "willpower"
    },
    {
        "id": 7,
        "title": "Утренняя рутина чемпиона",
        "description": "Создание идеального утреннего ритуала для максимальной продуктивности. Медитация, планирование дня, физическая активность и развитие mindset. Индивидуальные рекомендации по построению распорядка.",
        "category": "willpower"
    },
    {
        "id": 8,
        "title": "Фокус-марафон: 14 дней глубокой работы",
        "description": "Интенсивная программа по развитию способности к глубокой концентрации. Техники Помодоро, управление отвлечениями, создание идеальной рабочей среды. Для фрилансеров, студентов и профессионалов.",
        "category": "willpower"
    },

    # Творчество и хобби
    {
        "id": 9,
        "title": "Король и Шут: Гитарный фестиваль",
        "description": "Освой игру на гитаре легендарных песен 'Король и Шут' и покори фестиваль 20 мая. Пройди путь от базовых аккордов до полноценного сета из трех хитов. Видеоуроки, табы и обратная связь от опытных музыкантов.",
        "category": "creativity"
    },
    {
        "id": 10,
        "title": "Творческий дуэт: совместный арт-проект",
        "description": "Совместное создание творческого проекта за неделю. Парная работа над художественным произведением, развитие креативного мышления и навыков коллаборации. Подходит для друзей, пар или коллег.",
        "category": "creativity"
    },
    {
        "id": 11,
        "title": "Писательский интенсив: от идеи до рассказа",
        "description": "14-дневный курс по написанию короткого рассказа. Работа с персонажами, сюжетом, диалогами и стилем. Ежедневные письменные практики и групповое обсуждение работ.",
        "category": "creativity"
    },
    {
        "id": 12,
        "title": "Фотография для начинающих: взгляд художника",
        "description": "Освоение основ композиции, света и цвета в фотографии. Практические задания на улицах города, обработка в Lightroom, создание фотопортфолио. Подходит для смартфонов и зеркальных камер.",
        "category": "creativity"
    },

    # Ментальное здоровье
    {
        "id": 13,
        "title": "Путь к гармонии",
        "description": "14-дневный путь к внутреннему балансу и осознанности. Медитативные практики, ведение дневника эмоций, техники управления стрессом. Интеграция mindfulness в повседневную жизнь.",
        "category": "mental_health"
    },
    {
        "id": 14,
        "title": "Снижение тревожности: 21 день спокойствия",
        "description": "Научно-обоснованные методы работы с тревогой и беспокойством. Дыхательные техники, когнитивно-поведенческие упражнения, создание безопасного ментального пространства.",
        "category": "mental_health"
    },
    {
        "id": 15,
        "title": "Осознанные отношения: коммуникация без конфликтов",
        "description": "Развитие эмоционального интеллекта и навыков здорового общения. Практики активного слушания, выражения чувств, разрешения разногласий. Для пар, друзей и семей.",
        "category": "mental_health"
    },

    # Кулинария
    {
        "id": 16,
        "title": "Кулинарный дуэт: неделя гастрономических открытий",
        "description": "Неделя совместного кулинарного мастерства. Приготовление блюд разных кухонь мира, техники ножа, основы сочетания вкусов. Доставка свежих продуктов и онлайн-консультации шеф-повара.",
        "category": "cooking"
    },
    {
        "id": 17,
        "title": "Здоровое питание: с нуля до профи",
        "description": "Полный курс по приготовлению полезных и вкусных блюд. Баланс БЖУ, планирование меню, чтение этикеток, десерты без сахара. Для тех, кто хочет изменить пищевые привычки.",
        "category": "cooking"
    },
    {
        "id": 18,
        "title": "Выпечка для души: от круассанов до тортов",
        "description": "Мастер-классы по классической и современной выпечке. Дрожжевое тесто, безе, кремы и глазури. Подробные рецепты с пошаговыми видеоинструкциями.",
        "category": "cooking"
    },

    # Социальные навыки
    {
        "id": 19,
        "title": "The Friendship Forge",
        "description": "Embark on a journey to build meaningful connections and overcome social isolation. This quest will guide you through self-discovery, finding your tribe, and taking courageous steps to create lasting friendships. Weekly social challenges and community events.",
        "category": "social"
    },
    {
        "id": 20,
        "title": "Публичные выступления без страха",
        "description": "Преодоление глоссофобии и развитие ораторского мастерства. Техники подготовки речи, работа с голосом и телом, импровизация. Практика перед камерой и в мини-группах.",
        "category": "social"
    },
    {
        "id": 21,
        "title": "Networking для интровертов",
        "description": "Стратегии построения профессиональных связей для тех, кто не любит светские мероприятия. Целевые подходы, онлайн-нетворкинг, поддержание отношений. Кейсы из реальной бизнес-среды.",
        "category": "social"
    },

    # Личное развитие
    {
        "id": 22,
        "title": "Финансовая грамотность: управление бюджетом",
        "description": "Пошаговый план по контролю личных финансов. Составление бюджета, сокращение расходов, создание финансовой подушки, основы инвестирования. Приложения и инструменты для учета.",
        "category": "personal_development"
    },
    {
        "id": 23,
        "title": "Изучение английского через сериалы",
        "description": "Иммерсивный подход к изучению языка через популярные сериалы и фильмы. Работа с лексикой, идиомами, произношением. Интерактивные субтитры и разговорные клубы.",
        "category": "personal_development"
    },
    {
        "id": 24,
        "title": "Цифровая организация: порядок в устройствах",
        "description": "Систематизация цифровой жизни: файлы, почта, облачные сервисы, пароли. Оптимизация рабочего процесса и повышение кибербезопасности. Готовые шаблоны и чек-листы.",
        "category": "personal_development"
    }
]

# Тестовые пользователи с разными интересами
users_data = [
    {
        "user_id": 101,
        "quest_ids": [1, 4, 6]  # Фитнес-энтузиаст: здоровье + сила воли
    },
    {
        "user_id": 102,
        "quest_ids": [9, 10, 11, 12]  # Творческая личность: музыка, искусство, писательство, фото
    },
    {
        "user_id": 103,
        "quest_ids": [13, 14, 15]  # Фокус на ментальном здоровье
    },
    {
        "user_id": 104,
        "quest_ids": [16, 17, 18, 22]  # Кулинар + финансист
    },
    {
        "user_id": 105,
        "quest_ids": [5, 7, 8, 24]  # Продуктивность и организация
    },
    {
        "user_id": 106,
        "quest_ids": [2, 3, 19, 20]  # Разносторонний: здоровье + социальные навыки
    },
    {
        "user_id": 107,
        "quest_ids": [1, 5, 9, 13]  # Сбалансированный: по одному из каждой категории
    },
    {
        "user_id": 108,
        "quest_ids": []  # Новый пользователь без истории
    }
]


def print_divider(text):
    print(f"\n{'=' * 60}")
    print(f"🧪 {text}")
    print('=' * 60)


def test_api():
    print("🧪 Расширенное тестирование BERT API\n")

    # 1. Проверяем health (если есть эндпоинт)
    print_divider("1. Проверяем доступность API")
    try:
        # Если есть /api/health
        health_response = requests.get(f"{BASE_URL}/api/health")
        if health_response.status_code == 200:
            print(f"✅ API работает: {health_response.json()}")
    except:
        try:
            # Пробуем любой эндпоинт
            test_response = requests.get(f"{BASE_URL}/docs")
            print("✅ API доступен (документация загружается)")
        except Exception as e:
            print(f"❌ Не могу подключиться к API: {e}")
            print("Убедитесь, что сервер запущен: uvicorn main:app --host 0.0.0.0 --port 8000")
            return

    # 2. Добавляем квесты
    print_divider("2. Добавляем квесты в индекс")
    add_response = requests.post(
        f"{BASE_URL}/api/quests/add",
        json={"quests": quests_data}
    )

    if add_response.status_code == 200:
        result = add_response.json()
        print(f"✅ Добавлено квестов: {result.get('added', len(quests_data))}")
    else:
        print(f"❌ Ошибка добавления: {add_response.status_code}")
        print(f"Ответ: {add_response.text}")
        return

    # 3. Добавляем пользователей
    print_divider("3. Добавляем пользователей")
    add_users_response = requests.post(
        f"{BASE_URL}/api/users/add",
        json={"users": users_data}
    )

    if add_users_response.status_code == 200:
        result = add_users_response.json()
        print(f"✅ Добавлено пользователей: {result.get('total_users', len(users_data))}")
        print(f"✅ Создано профилей: {result.get('total_profiles', 0)}")
        print(f"📊 Сообщение: {result.get('message', '')}")
    else:
        print(f"❌ Ошибка добавления пользователей: {add_users_response.status_code}")
        print(f"Ответ: {add_users_response.text}")

    time.sleep(2)  # Даем время на обработку

    # 4. Тестируем поиск с разными запросами
    print_divider("4. Тестируем семантический поиск")

    test_queries = [
        ("бег и физические упражнения", None),
        ("утренние привычки и медитация", "willpower"),
        ("гитара музыка творчество", "creativity"),
        ("кулинария готовка еда", "cooking"),
        ("тревога стресс спокойствие", "mental_health"),
        ("продуктивность работа фокус", None),
        ("общение друзья социальные навыки", "social"),
        ("финансы деньги бюджет", "personal_development"),
        ("фотография искусство визуальное", None),
        ("йога растяжка гибкость", "health")
    ]

    for query, category in test_queries:
        print(f"\n🔍 Запрос: '{query}'" + (f" [категория: {category}]" if category else ""))

        search_data = {
            "query": query,
            "top_k": 3,
            "category": category
        }

        try:
            search_response = requests.post(
                f"{BASE_URL}/api/search",
                json=search_data,
                timeout=10
            )

            if search_response.status_code == 200:
                results = search_response.json()
                print(f"   ⏱️ Время поиска: {results.get('search_time_ms', 'N/A')} мс")

                if results.get('results'):
                    for i, result in enumerate(results['results'], 1):
                        similarity = result.get('similarity_score', 0)
                        print(f"   {i}. [{result.get('category', 'N/A')}] {result['title']}")
                        print(f"      Схожесть: {similarity:.3f}")
                else:
                    print("   ❌ Нет результатов")
            else:
                print(f"   ❌ Ошибка: {search_response.status_code}")

        except Exception as e:
            print(f"   ❌ Исключение: {e}")

    # 5. Тестируем поиск похожих квестов
    print_divider("5. Тестируем поиск похожих квестов")

    similar_tests = [
        (1, "Утренний дружеский марафон"),
        (9, "Король и Шут: Гитарный фестиваль"),
        (16, "Кулинарный дуэт"),
        (13, "Путь к гармонии")
    ]

    for quest_id, quest_name in similar_tests:
        print(f"\n🔍 Похожие на: '{quest_name}' (ID: {quest_id})")

        similar_data = {
            "quest_id": quest_id,
            "top_k": 4
        }

        try:
            similar_response = requests.post(
                f"{BASE_URL}/api/similar",
                json=similar_data,
                timeout=10
            )

            if similar_response.status_code == 200:
                results = similar_response.json()
                similar_quests = results.get('similar_quests', [])

                if similar_quests:
                    for i, quest in enumerate(similar_quests, 1):
                        print(f"   {i}. [{quest.get('category', 'N/A')}] {quest['title']}")
                        print(f"      Схожесть: {quest.get('similarity_score', 0):.3f}")
                else:
                    print("   ❌ Нет похожих квестов")
            else:
                print(f"   ❌ Ошибка: {similar_response.status_code}")

        except Exception as e:
            print(f"   ❌ Исключение: {e}")

    # 6. Тестируем рекомендации квестов для пользователей
    print_divider("6. Тестируем рекомендации квестов")

    recommendation_tests = [
        (101, "Фитнес-энтузиаст", None),
        (102, "Творческая личность", "creativity"),
        (103, "Фокус на ментальном здоровье", "mental_health"),
        (104, "Кулинар + финансист", None),
        (105, "Гуру продуктивности", "willpower"),
        (108, "Новый пользователь (без истории)", None)
    ]

    for user_id, user_type, category in recommendation_tests:
        print(f"\n🎯 Рекомендации для: {user_type} (ID: {user_id})" +
              (f" [категория: {category}]" if category else ""))

        # Получаем quest_ids пользователя
        user_quest_ids = []
        for user in users_data:
            if user["user_id"] == user_id:
                user_quest_ids = user["quest_ids"]
                break

        print(f"   История пользователя: {user_quest_ids if user_quest_ids else 'пусто'}")

        recommend_data = {
            "user_quest_ids": user_quest_ids,
            "top_k": 5,
            "category": category
        }

        try:
            recommend_response = requests.post(
                f"{BASE_URL}/api/quests/recommend",
                json=recommend_data,
                timeout=10
            )

            if recommend_response.status_code == 200:
                results = recommend_response.json()
                recommendations = results.get('recommendations', [])
                message = results.get('message', '')

                if message:
                    print(f"   📝 {message}")

                if recommendations:
                    print(f"   🎁 Найдено рекомендаций: {len(recommendations)}")
                    for i, rec in enumerate(recommendations, 1):
                        explanation = rec.get('explanation', '')
                        print(f"   {i}. [{rec.get('category', 'N/A')}] {rec['title']}")
                        print(f"      Схожесть: {rec.get('similarity_score', 0):.3f}")
                        if explanation:
                            print(f"      💡 {explanation[:80]}...")
                else:
                    print("   ❌ Нет рекомендаций")

                if results.get('user_profile_info'):
                    info = results['user_profile_info']
                    print(f"   📊 Инфо: {info['quests_count']} квестов, dim={info['embedding_dim']}")

            else:
                print(f"   ❌ Ошибка: {recommend_response.status_code}")
                print(f"   Ответ: {recommend_response.text}")

        except Exception as e:
            print(f"   ❌ Исключение: {e}")

    # 7. Тестируем рекомендации пользователей
    print_divider("7. Тестируем рекомендации похожих пользователей")

    user_recommendation_tests = [101, 102, 103, 104, 105]

    for user_id in user_recommendation_tests:
        print(f"\n👥 Рекомендации пользователей для: ID {user_id}")

        recommend_users_data = {
            "user_id": user_id,
            "top_k": 3
        }

        try:
            recommend_response = requests.post(
                f"{BASE_URL}/api/users/recommend",
                json=recommend_users_data,
                timeout=10
            )

            if recommend_response.status_code == 200:
                results = recommend_response.json()
                user_results = results.get('results', [])

                if user_results:
                    for i, user_rec in enumerate(user_results, 1):
                        user_id_rec = user_rec.get('user_id', 'N/A')
                        similarity = user_rec.get('similarity_score', 0)

                        # Находим имя пользователя
                        user_name = "Неизвестный"
                        for user in users_data:
                            if user["user_id"] == user_id_rec:
                                quest_count = len(user["quest_ids"])
                                interests = []
                                for qid in user["quest_ids"][:3]:
                                    for quest in quests_data:
                                        if quest["id"] == qid:
                                            interests.append(quest["category"])
                                            break
                                user_name = f"Пользователь с {quest_count} квестами ({', '.join(set(interests))})"
                                break

                        print(f"   {i}. ID {user_id_rec}: {user_name}")
                        print(f"      Схожесть профилей: {similarity:.3f}")
                else:
                    print("   ❌ Нет рекомендаций пользователей")
            else:
                print(f"   ❌ Ошибка: {recommend_response.status_code}")

        except Exception as e:
            print(f"   ❌ Исключение: {e}")

    # 8. Комплексный тест: симуляция пользовательского сценария
    print_divider("8. Комплексный тест: пользовательский сценарий")

    print("\n🎬 Сценарий: Новый пользователь ищет квесты и получает рекомендации")

    # Шаг 1: Поиск квестов по интересам
    print("\n1. Пользователь ищет 'йога и медитация':")
    search_data = {
        "query": "йога и медитация для расслабления",
        "top_k": 3,
        "category": None
    }

    search_response = requests.post(f"{BASE_URL}/api/search", json=search_data)
    if search_response.status_code == 200:
        results = search_response.json()
        for i, result in enumerate(results.get('results', [])[:2], 1):
            print(f"   Найден квест {i}: {result['title']} (схожесть: {result.get('similarity_score', 0):.3f})")

    # Шаг 2: Пользователь проходит несколько квестов
    print("\n2. Пользователь проходит квесты [3, 13, 14] (йога + ментальное здоровье)")

    # Шаг 3: Получаем рекомендации на основе пройденных квестов
    recommend_data = {
        "user_quest_ids": [3, 13, 14],  # Йога + ментальное здоровье
        "top_k": 4,
        "category": None
    }

    recommend_response = requests.post(f"{BASE_URL}/api/quests/recommend", json=recommend_data)
    if recommend_response.status_code == 200:
        results = recommend_response.json()
        print("   Рекомендации системы:")
        for i, rec in enumerate(results.get('recommendations', [])[:3], 1):
            print(f"   {i}. {rec['title']} (категория: {rec.get('category', 'N/A')})")
            print(f"      Объяснение: {rec.get('explanation', 'Нет объяснения')[:60]}...")

    print("\n" + "=" * 60)
    print("✅ Тестирование завершено!")
    print("=" * 60)


def run_performance_test():
    """Тест производительности"""
    print("\n⚡ Запускаем тест производительности")

    # Проверяем, что квесты добавлены
    if not quests_data:
        print("❌ Нет данных для тестирования")
        return

    # Тестируем скорость поиска
    queries = ["фитнес", "творчество", "продуктивность", "кулинария", "медитация"]

    total_time = 0
    successful_searches = 0

    for query in queries:
        search_data = {
            "query": query,
            "top_k": 5,
            "category": None
        }

        start_time = time.time()
        try:
            response = requests.post(f"{BASE_URL}/api/search", json=search_data, timeout=5)
            if response.status_code == 200:
                search_time = (time.time() - start_time) * 1000
                total_time += search_time
                successful_searches += 1
                print(f"  Запрос '{query}': {search_time:.2f} мс")
        except:
            continue

    if successful_searches > 0:
        avg_time = total_time / successful_searches
        print(f"\n📊 Среднее время поиска: {avg_time:.2f} мс")
        print(f"📊 Успешных запросов: {successful_searches}/{len(queries)}")

        if avg_time < 100:
            print("✅ Производительность отличная!")
        elif avg_time < 300:
            print("⚠️  Производительность приемлемая")
        else:
            print("❌ Производительность низкая")
    else:
        print("❌ Не удалось выполнить тест производительности")


if __name__ == "__main__":
    try:
        test_api()
        # run_performance_test()  # Раскомментировать для теста производительности
    except KeyboardInterrupt:
        print("\n\n⚠️ Тестирование прервано пользователем")
    except Exception as e:
        print(f"\n❌ Критическая ошибка: {e}")
        import traceback

        traceback.print_exc()